<form method="post" action="{{ path('ordreachat_delete', {'id': ordreachat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ordreachat.id) }}">
    <button class="btn">Delete</button>
</form>

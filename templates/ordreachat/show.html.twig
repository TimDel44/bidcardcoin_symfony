{% extends 'base.html.twig' %}

{% block title %}Ordreachat{% endblock %}

{% block body %}
    <h1>Ordreachat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ordreachat.id }}</td>
            </tr>
            <tr>
                <th>MontantMax</th>
                <td>{{ ordreachat.montantMax }}</td>
            </tr>
            <tr>
                <th>AdresseDepot</th>
                <td>{{ ordreachat.adresseDepot }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ordreachat_index') }}">back to list</a>

    <a href="{{ path('ordreachat_edit', {'id': ordreachat.id}) }}">edit</a>

    {{ include('ordreachat/_delete_form.html.twig') }}
{% endblock %}
